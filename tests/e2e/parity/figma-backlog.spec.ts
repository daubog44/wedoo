import { test } from "@playwright/test";

test.describe("figma parity backlog", () => {
  test("Landing page matches Figma frame 143:1822", async () => {
    test.fixme();
  });

  test("Login error state matches Figma frame 658:667", async () => {
    test.fixme();
  });

  test("Candidate education modal matches Figma frame 280:1079", async () => {
    test.fixme();
  });

  test("Candidate work experience modal matches Figma frame 280:860", async () => {
    test.fixme();
  });

  test("Candidate skills modal matches Figma frame 280:951", async () => {
    test.fixme();
  });

  test("Candidate contacts modal matches Figma frame 281:1207", async () => {
    test.fixme();
  });

  test("Company job wizard step 1 matches Figma frame 258:847", async () => {
    test.fixme();
  });

  test("Company job wizard step 2 matches Figma frame 259:1050", async () => {
    test.fixme();
  });
});
