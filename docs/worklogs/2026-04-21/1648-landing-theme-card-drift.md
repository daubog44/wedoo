## 1648 landing-theme-card-drift

- 16:39 - task `/` reopen audit - node `143:1822` - touched area landing header + hero - tests run `none` - user screenshot shows missing theme toggle on home and a low-contrast hero card with white display text on a near-white panel - next step inspect PRD state, Figma source and local route.
- 16:42 - task `/` reopen audit - node `143:1822` - touched area Figma landing reference - tests run `mcp figma get_metadata/get_design_context/get_screenshot` - Figma still describes a single coherent landing canvas and does not justify the current split dark shell with a washed-out light hero slab; the only top-right control is the language chip in the original frame - next step capture the real route on the live dev server and compare.
- 16:43 - task `/` reopen audit - node `143:1822` - touched area landing real browser output - tests run `WEDOO_DEV_PORT=4601 npm run loop:capture -- / landing-toggle-card-drift` - real capture confirms the landing does not render `WedooThemeToggle` in the header at all and the hero left panel is illegible against the active dark theme - next step reopen a single FRAME task in `prd.md`, mirror it in `docs/visual-backlog.md`, then stop the round because PRD was empty before this drift evidence.
- 16:48 - task `/` implementation - node `143:1822` viewport `desktop` - touched area landing header + hero surfaces + theme tokens - tests run `npm run typecheck` - rebuilt the home hero into two theme-governed panels, mounted the shared `WedooThemeToggle` in the landing top bar, darkened light-theme muted tokens, and added a dedicated preview-copy backdrop; inference: the original Figma frame has only the language chip, so the toggle was intentionally added as a shared system control without preserving the broken split-card layout - next step validate route behavior and captures.
- 16:53 - task `/` validation - node `143:1822` viewport `desktop + mobile` - touched area landing route behavior + theme persistence - tests run `WEDOO_DEV_PORT=4601 npx playwright test tests/e2e/public/landing-page.spec.ts tests/e2e/public/theme-toggle.spec.ts`, `WEDOO_DEV_PORT=4601 npm run loop:capture -- / landing-theme-card-final`, custom Playwright light capture to `artifacts/loop-captures/2026-04-21/1654-landing-theme-card-light` - dark and light captures both show readable hero copy and visible toggle on home; the workspace preview no longer loses contrast in light mode - next step refresh landing parity intentionally and rerun green.
- 16:56 - task `/` closeout - node `143:1822` viewport `desktop + mobile` - touched area parity + tracker sync - tests run `WEDOO_DEV_PORT=4601 npx playwright test tests/e2e/parity/landing-page.visual.spec.ts --update-snapshots`, then rerun without update - landing parity is stable again after the intentional baseline refresh; task closed in `prd.md` and moved to resolved in `docs/visual-backlog.md` with final dark/light capture paths `1653-landing-theme-card-final` and `1654-landing-theme-card-light`.
